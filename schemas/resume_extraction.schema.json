{
  "$defs": {
    "EvidenceKind": {
      "description": "Closed classification of one confirmed evidence item.\n\nShared by ``ExperienceItem.kind`` (user-profile spec) and\n``EvidenceSlot.required_kinds`` (pathway-template spec). Closed for humans\ntoo: kinds are join keys for the deterministic ``narrative/`` kernel, so\nthe UI offers the same fixed dropdown the LLM proposal is bound to.",
      "enum": [
        "work",
        "project",
        "volunteering",
        "leadership",
        "research",
        "award",
        "coursework"
      ],
      "title": "EvidenceKind",
      "type": "string"
    },
    "ExperienceItem": {
      "additionalProperties": false,
      "description": "One confirmed evidence entry (work, project, volunteering, ...).\n\nProfile vocabulary: lives here because the profile owns the confirmed\nvalues; ``resume_extraction`` (the ResumeIntakeNode proposal) imports it\nrather than redeclaring.\n\nThe field name (and the profile's ``experience`` list name) predates the\nstory layer and deliberately stays: ``kind`` / ``theme_tags`` landed as an\nadditive amendment, not a rename/migration (narrative-pathways NP-A).\n``theme_tags`` membership in the registry theme vocabulary is a\nservice-layer check, not a contract-shape check.",
      "properties": {
        "kind": {
          "$ref": "#/$defs/EvidenceKind",
          "default": "work"
        },
        "organization": {
          "anyOf": [
            {
              "maxLength": 120,
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Organization"
        },
        "summary": {
          "anyOf": [
            {
              "maxLength": 280,
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Summary"
        },
        "theme_tags": {
          "items": {
            "maxLength": 60,
            "minLength": 1,
            "type": "string"
          },
          "maxItems": 5,
          "title": "Theme Tags",
          "type": "array"
        },
        "title": {
          "maxLength": 120,
          "minLength": 1,
          "title": "Title",
          "type": "string"
        }
      },
      "required": [
        "title"
      ],
      "title": "ExperienceItem",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Proposed profile-field candidates from one r\u00e9sum\u00e9 extraction.",
  "properties": {
    "experience": {
      "items": {
        "$ref": "#/$defs/ExperienceItem"
      },
      "maxItems": 20,
      "title": "Experience",
      "type": "array"
    },
    "inferred_weak_spots": {
      "items": {
        "maxLength": 60,
        "minLength": 1,
        "type": "string"
      },
      "maxItems": 15,
      "title": "Inferred Weak Spots",
      "type": "array"
    },
    "known_strengths": {
      "items": {
        "maxLength": 60,
        "minLength": 1,
        "type": "string"
      },
      "maxItems": 15,
      "title": "Known Strengths",
      "type": "array"
    },
    "skills": {
      "items": {
        "maxLength": 60,
        "minLength": 1,
        "type": "string"
      },
      "maxItems": 40,
      "title": "Skills",
      "type": "array"
    },
    "target_company_categories": {
      "items": {
        "maxLength": 60,
        "minLength": 1,
        "type": "string"
      },
      "maxItems": 8,
      "title": "Target Company Categories",
      "type": "array"
    }
  },
  "title": "ResumeExtraction",
  "type": "object"
}
